New Features:
- Settings are now persistent and saved automatically
- Automatically checks for new updates on startup
- Now available as a standalone portable executable
- Modern dark UI with teal accent colors
- Model presets dropdown with 50+ NVIDIA NIM models
- Custom searchable model dropdown with keyboard navigation
- Max Tokens slider + input field for precise control
- Temperature slider (now supports up to 2.0)
- Tunnel URL automatically displays /v1/chat/completions endpoint
- Pass-through API parameters (client settings override NIMB defaults)
- Windows-style custom titlebar with crisp SVG icons
- Real-time statistics dashboard with uptime counter
- Detailed error logging with timestamps
